CREATE SEQUENCE info_id_seq AS BIGINT START WITH 1 INCREMENT BY 1;
CREATE TABLE info (
  id BIGINT GENERATED BY DEFAULT AS SEQUENCE info_id_seq PRIMARY KEY,
  logic INTEGER(20) NOT NULL,
  serial VARCHAR(80) NOT NULL,
  model VARCHAR(11) NOT NULL
);